

CREATE TABLE IF  NOT EXISTS model_run_instance (
	id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	batch_id BIGINT , 
	input_value_id BIGINT,
	output_value_id BIGINT

);

CREATE TABLE IF NOT EXISTS model_run_inputs(
	id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	model_inputs VARChAR(4096)
);
CREATE TABLE IF NOT EXISTS model_inputs(
	model_input_id BIGINT,
	name VARCHAR(4096),
	property VARCHAR(4096)
);

CREATE TABLE IF NOT EXISTS mdoel_run_outputs (
	id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	model_outputs VARCHAR(4096)
);

CREATE TABLE IF NOT EXISTS model_outputs(
	model_output_id BIGINT,
	name VARCHAR(4096),
	property VARCHAR(4096)
);

CREATE TABLE IF NOT EXISTS batch_job(
	
	id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	model_id BIGINT,
	status VARCHAR(32)
	
);

CREATE TABLE IF NOT EXISTS models(
	id  BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	name VARCHAR(4096),
	description VARCHAR(4096)
);

